# Produktions-Konfiguration für Klipz

# Standard API Base (über Cloudflare)
# Wenn nicht gesetzt, wird http://localhost:${VITE_BACKEND_PORT} verwendet
VITE_API_BASE=https://example.com

# Upload Base (alternative Domain ohne Cloudflare für große Dateien)
# Wenn nicht gesetzt, wird VITE_API_BASE verwendet
VITE_UPLOAD_BASE=https://upload.example.com:3301

# Registrierung aktivieren/deaktivieren (true = aktiviert, false = deaktiviert)
VITE_ENABLE_REGISTRATION=true

# Port-Konfiguration
# Frontend Port (Vite Development Server)
VITE_FRONTEND_PORT=5173

# Backend Port (Express Server)
VITE_BACKEND_PORT=3000

# Upload Server Port (für große Dateien)
VITE_UPLOAD_PORT=3301